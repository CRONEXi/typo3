name: CI for TYPO3 Project

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  print-message:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Print Hello, World!
        run: echo "Hello, World!"
